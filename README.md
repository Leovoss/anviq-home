# Anviq site

Static one-page marketing site for Anviq, built from the Phase 4 brand handbook and design tokens (colors, type scale, spacing, logo mark).

No build step. Deploy on Cloudflare Pages: connect this repo, leave the build command empty, set the output directory to `/`.
